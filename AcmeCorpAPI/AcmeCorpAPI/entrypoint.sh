# Start the API
dotnet AcmeCorpAPI.dll

# Add other dependent resources setup here if needed
# For example, start other services, initialize caches, etc.

# Keep the script running to keep the container alive
tail -f /dev/null